# Простая процедура без параметров
Процедура Тест()
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList))

# Процедура без параметров с блоком кода
Процедура Обработать()
  а = 1 + 2;
  б = 3;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList,Block(...)))

# Процедура с одним параметром
Процедура Увеличить(Значение)
  Значение = Значение + 1;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name)),Block(...)))

# Процедура с несколькими параметрами
Процедура Обменять(А, Б)
  временное = А;
  А = Б;
  Б = временное;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name),Param(Name)),Block(...)))

# Процедура с параметром по значению (Знач)
Процедура Тест(Знач Параметр)
  Параметр = 1;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Val,Name)),Block(...)))

# Процедура с параметрами по значению и без
Процедура Тест(Параметр1, Знач Параметр2)
  Параметр1 = 1;
  Параметр2 = 2;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name),Param(Val,Name)),Block(...)))

# Процедура с параметром по умолчанию
Процедура Тест(Параметр = 10)
  Параметр = Параметр + 1;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name,DefaultValue(...))),Block(...)))

# Процедура с Экспорт
Процедура ПубличнаяПроцедура() Экспорт
  а = 1;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList,Export,Block(...)))

# Асинхронная процедура
Асинх Процедура Тест()
  а = 1;
КонецПроцедуры
==>
Module(ProcedureDef(Async,Name,ParamList,Block(...)))

# Процедура с аннотацией
&НаСервере
Процедура Тест()
  а = 1;
КонецПроцедуры
==>
Module(ProcedureDef(Annotation(AnnotationType),Name,ParamList,Block(...)))

# Процедура с несколькими аннотациями
&Перед("Тест")
&НаСервере
&НаСервере
Процедура Тест()
  а = 1;
КонецПроцедуры
==>
Module(ProcedureDef(Annotation(AnnotationType,String),Annotation(AnnotationType),Annotation(AnnotationType),Name,ParamList,Block(...)))

# Процедура с условным оператором
Процедура Тест(условие)
  Если условие Тогда
    а = 1;
  КонецЕсли;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name)),Block(...)))

# Процедура с циклом
Процедура Тест()
  Пока Истина Цикл
    а = а + 1;
  КонецЦикла;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList,Block(...)))

# Процедура с обработкой исключений
Процедура Тест(а, б)
  Попытка
    результат = а / б;
  Исключение
    результат = 0;
  КонецПопытки;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name),Param(Name)),Block(...)))

# Процедура с вызовом другой процедуры
Процедура Тест(параметр)
  ВызватьПроцедуру(параметр);
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name)),Block(...)))

# Процедура с несколькими операторами
Процедура Тест(параметр)
  а = параметр + 1;
  б = параметр + 2;
  ВызватьПроцедуру(а);
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name)),Block(...)))

# Процедура с пустым блоком и несколькими пустыми строками
Процедура Тест()



КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList))

# Процедура с одним параметром и пустым телом
Процедура Тест(параметр)
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name))))

# Процедура с тремя параметрами
Процедура Тест(а, б, в)
  результат = (а + б) * в;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name),Param(Name),Param(Name)),Block(...)))

# Процедура с параметрами разных типов (Знач и обычные)
Процедура Тест(а, Знач б, в = 5)
  результат = а + б + в;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name),Param(Val,Name),Param(Name,DefaultValue(...))),Block(...)))

# Процедура с вызовом метода объекта
Процедура Тест(объект)
  объект.Метод();
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name)),Block(...)))

# Процедура с доступом к свойству
Процедура Тест(объект, значение)
  объект.Свойство = значение;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name),Param(Name)),Block(...)))

# Процедура с элементом массива
Процедура Тест(массив, индекс, значение)
  массив[индекс] = значение;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name),Param(Name),Param(Name)),Block(...)))

# Процедура с созданием объекта
Процедура Тест()
  объект = Новый Массив;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList,Block(...)))

# Процедура с тернарным выражением
Процедура Тест(а, б)
  результат = ?(а > б, а, б);
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name),Param(Name)),Block(...)))

# Процедура без операторов в теле
Процедура Тест()
  а = 1
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList,Block(...)))

# Несколько процедур в модуле
Процедура Тест1()
  а = 1;
КонецПроцедуры

Процедура Тест2()
  б = 2;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList,Block(...)),ProcedureDef(Name,ParamList,Block(...)))

# Процедура с параметром по умолчанию (Неопределено)
Процедура Тест(параметр = Неопределено)
  Если параметр = Неопределено Тогда
    параметр = 1;
  КонецЕсли;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name,DefaultValue(...))),Block(...)))

# Процедура с параметром по умолчанию (Null)
Процедура Тест(параметр = Null)
  Если параметр = Null Тогда
    параметр = "значение";
  КонецЕсли;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name,DefaultValue(...))),Block(...)))

# Процедура с параметром по умолчанию (дата)
Процедура Тест(дата = '20240101')
  Сообщить(дата);
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name,DefaultValue(...))),Block(...)))

# Процедура с логическим выражением в условии
Процедура Тест(а, б)
  Если а > 0 И б > 0 Тогда
    Сообщить("Положительные");
  Иначе
    Сообщить("Не все положительные");
  КонецЕсли;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name),Param(Name)),Block(...)))

# Процедура с вложенными условиями
Процедура Тест(значение)
  Если значение > 10 Тогда
    Если значение > 20 Тогда
      Сообщить("Больше 20");
    Иначе
      Сообщить("От 10 до 20");
    КонецЕсли;
  Иначе
    Сообщить("Меньше или равно 10");
  КонецЕсли;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name)),Block(...)))

# Процедура с циклом Пока
Процедура Тест()
  счетчик = 0;
  Пока счетчик < 10 Цикл
    счетчик = счетчик + 1;
  КонецЦикла;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList,Block(...)))

# Процедура с циклом Для
Процедура Тест(максимум)
  Для индекс = 1 По максимум Цикл
    Сообщить(индекс);
  КонецЦикла;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name)),Block(...)))

# Процедура с оператором Продолжить
Процедура Тест(массив)
  Для Каждого элемент Из массив Цикл
    Если (элемент % 2) = 0 Тогда
      Продолжить;
    КонецЕсли;
    Сообщить(элемент);
  КонецЦикла;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name)),Block(...)))

# Процедура с оператором Прервать
Процедура Тест(массив, искомое)
  Для Каждого элемент Из массив Цикл
    Если элемент = искомое Тогда
      Сообщить(элемент);
      Прервать;
    КонецЕсли;
  КонецЦикла;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name),Param(Name)),Block(...)))

# Процедура с оператором ВызватьИсключение
Процедура Тест(параметр)
  Если параметр = Неопределено Тогда
    ВызватьИсключение;
  КонецЕсли;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name)),Block(...)))

# Процедура с оператором Выполнить
Процедура Тест(код)
  Выполнить код;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name)),Block(...)))

# Процедура с оператором Перейти
Процедура Тест()
  Перейти ~метка;
  ~метка:
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList,Block(...)))

# Процедура с обработчиками событий
Процедура Тест(объект)
  ДобавитьОбработчик объект.Событие, обработчик;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name)),Block(...)))

# Процедура с удалением обработчика
Процедура Тест(объект)
  УдалитьОбработчик объект.Событие, обработчик;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name)),Block(...)))

# Процедура с асинхронным вызовом
Асинх Процедура Тест()
  результат = Ждать ВызватьФункцию();
КонецПроцедуры
==>
Module(ProcedureDef(Async,Name,ParamList,Block(...)))

# Процедура с аннотацией, асинхронная и экспортная
&НаСервере
Асинх Процедура Тест() Экспорт
  а = 1;
КонецПроцедуры
==>
Module(ProcedureDef(Annotation(AnnotationType),Async,Name,ParamList,Export,Block(...)))

# Процедура с множественными параметрами по умолчанию
Процедура Тест(а = 1, б = 2, в = 3)
  результат = а + б + в;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name,DefaultValue(...)),Param(Name,DefaultValue(...)),Param(Name,DefaultValue(...))),Block(...)))

# Процедура с параметром по умолчанию (строка с пробелами)
Процедура Тест(сообщение = "Привет мир")
  Сообщить(сообщение);
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name,DefaultValue(...))),Block(...)))

# Процедура с вызовом функции в выражении
Процедура Тест(значение)
  результат = ПолучитьЗначение(значение) + 10;
  Сообщить(результат);
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name)),Block(...)))

# Процедура со сложным выражением
Процедура Тест(а, б, в)
  результат = (а + б) * в + 5;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name),Param(Name),Param(Name)),Block(...)))

# Процедура с вложенными вызовами методов
Процедура Тест(объект)
  объект.Метод1().Метод2().Свойство = значение;
КонецПроцедуры
==>
Module(ProcedureDef(Name,ParamList(Param(Name)),Block(...)))
