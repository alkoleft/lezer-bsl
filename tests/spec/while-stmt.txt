# Простой цикл пока
Пока Истина Цикл
КонецЦикла;
==>
Module(ModuleBlock(
  WhileStmt(Condition(True))))

# Цикл пока с блоком
Пока Истина Цикл
  а = 1;
КонецЦикла;
==>
Module(ModuleBlock(WhileStmt(
  Condition(True),
  Block(AssignmentStmt(...)))))

# Цикл пока с условием
Пока а < 10 Цикл
  а = а + 1;
КонецЦикла;
==>
Module(ModuleBlock(WhileStmt(
  Condition(BinaryExpr(...)),
  Block(AssignmentStmt(...)))))

# Цикл пока с несколькими операторами
Пока Истина Цикл
  а = 1;
  б = 2;
  в = 3;
КонецЦикла;
==>
Module(ModuleBlock(WhileStmt(
  Condition(...),
  Block(
    AssignmentStmt(...),
    AssignmentStmt(...),
    AssignmentStmt(...)))))

# Цикл пока с логическим выражением
Пока а > 0 И б < 10 Цикл
  а = а - 1;
КонецЦикла;
==>
Module(ModuleBlock(WhileStmt(
  Condition(BinaryExpr(...)),
  Block(AssignmentStmt(...)))))

# Цикл пока с вызовом функции в условии
Пока Функция1() Цикл
  а = 1;
КонецЦикла;
==>
Module(ModuleBlock(WhileStmt(
  Condition(CallExpr(...)),
  Block(AssignmentStmt(...)))))

# Цикл пока на английском
While True Do
  a = 1;
EndDo;
==>
Module(ModuleBlock(WhileStmt(
  Condition(...),
  Block(AssignmentStmt(...)))))

# Вложенный цикл пока
Пока Истина Цикл
  Пока Истина Цикл
    а = 1;
  КонецЦикла;
КонецЦикла;
==>
Module(ModuleBlock(WhileStmt(
  Condition(...),
  Block(WhileStmt(
    Condition(...),
    Block(AssignmentStmt(...)))))))

# Цикл пока с условным оператором
Пока Истина Цикл
  Если а > 0 Тогда
    а = а - 1;
  КонецЕсли;
КонецЦикла;
==>
Module(ModuleBlock(WhileStmt(
  Condition(True),
  Block(IfStmt(
    Condition(...),
    Block(AssignmentStmt(...)))))))

# Цикл пока с переменной в условии
Пока флаг Цикл
  флаг = Ложь;
КонецЦикла;
==>
Module(ModuleBlock(WhileStmt(
  Condition(VariableName),
  Block(AssignmentStmt(...)))))
